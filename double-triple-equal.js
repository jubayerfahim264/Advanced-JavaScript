const fNumber = 20;
const sNumber = "20";
if (fNumber === sNumber) {
  console.log("condition is true");
} else {
  console.log("condition is false");
}
// == it's means it will check only value & === it's means it will check value with data type.
